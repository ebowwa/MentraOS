# Shared MCUboot sysbuild configuration
# Configure MCUboot for 3MB XIP partitions

# Critical: Increase max sectors for 3MB images
# 3MB / 4KB = 768 sectors, use 800 for safety
CONFIG_BOOT_MAX_IMG_SECTORS=800

# Enable external flash secondary slot
CONFIG_PM_EXTERNAL_FLASH_MCUBOOT_SECONDARY=y